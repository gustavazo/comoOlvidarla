import Cursos from '../screens/Cursos';
import Layout from '../components/Layout';
export const config = { amp: true }

export default () => {
    return (
      <Layout>
          <Cursos />
      </Layout>  
        
    )
        
      
}